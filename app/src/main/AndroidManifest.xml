<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.latihanandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.List_activity"></activity>
        <activity android:name=".activity.UpdateActivity" />
        <activity android:name=".activity.cobarecyclelagi" />
        <activity
            android:name=".activity.Recycler2"
            android:label="@string/title_activity_recycler2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.tambahitemsekolah" />
        <activity android:name=".activity.melihatsekolah" />
        <activity android:name=".activity.MapADS" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>